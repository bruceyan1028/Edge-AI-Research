> 本文件为《端侧模型技术路线与指标对比·数据支撑清单》的中间产物，合并进正式清单后可删除。
> 检索日期：2026-09-09。所有"官方口径"均指厂商自报数字。

---

# 美国端侧语音/音频模型技术规格调研报告

调研时间 2026-09-09。所有数据均来自一手源（openai/whisper repo、whisper.cpp repo、HuggingFace 模型卡、arXiv、NVIDIA 模型卡）。未公开的字段一律写"未披露"。

---

## 一、OpenAI Whisper（全尺寸阶梯）

| 字段 | 内容 |
|---|---|
| 1. 全名/版本 | Whisper tiny / base / small / medium / large-v1 / large-v2 / large-v3 / large-v3-turbo（`turbo`） |
| 2. 公司 | OpenAI（美国，旧金山） |
| 3. 首发 | 2022-09 初版发布；large-v2 = 2022-12（tag `v20221205`，**注：该 tag 未在本次抓取的 GitHub releases 第 1/2 页中直接出现，属未一手验证**）；**large-v3 = 2023-11**（release tag `v20231106`，2023-11-06，DevDay）；**large-v3-turbo = 2024-09-30**（release tag `v20240930`，publish 时间 30 Sep 18:21） |
| 4. 参数量 | tiny 39M / base 74M / small 244M / medium 769M / large 1550M / turbo 809M（官方 README 表格） |
| 5. 架构 | encoder-decoder Transformer（seq2seq，log-Mel 频谱输入）。large-v3 相对 v2 仅两处改动："The spectrogram input uses 128 Mel frequency bins instead of 80" + 新增粤语 language token。turbo = **对 large-v3 剪枝后微调**，"the number of decoding layers have reduced from 32 to 4"，再在 large-v3 同款多语数据上多训 2 个 epoch（**不是蒸馏**，但官方明确说灵感来自 Distil-Whisper 的"小 decoder"发现） |
| 6. 蒸馏/压缩自 | large-v3-turbo ← **openai/whisper-large-v3**（剪枝+微调）。其余尺寸为独立预训练 |
| 7. 量化体积 | 原生权重非量化（F16）。量化体积见下方 whisper.cpp 章节 |
| 8. 上下文/音频窗 | **固定 30 秒感受野**。"Whisper has a receptive field of 30-seconds."；长音频靠 `transcribe()` 的 "sliding 30-second window, performing autoregressive sequence-to-sequence predictions on each window"；HF 侧提供 sequential（滑窗缓冲）与 chunked（切片+重叠拼接）两种；官方称 large-v3 的最优 chunk 长度就是 30 秒 |
| 9. 许可 | **MIT**（GitHub README："code and weights are released under the MIT License"；whisper-large-v3-turbo HF 卡 license 字段 = mit）。**口径冲突：openai/whisper-large-v3 的 HF 卡 license 字段写的是 apache-2.0**，与 repo 的 MIT 不一致 —— 做表时建议以 repo MIT 为准并加注 |
| 10. 官方端侧性能 | **官方 README 的 "Relative speed" 列：tiny ~10x / base ~7x / small ~4x / medium ~2x / large 1x / turbo ~8x。口径：在 A100 上转写英文测得**（"benchmarked on an A100 transcribing English"），README 自己警告实际吞吐随语言、语速、硬件而变。**这是数据中心 GPU 数字，不是端侧数字。** VRAM 需求：~1GB/~1GB/~2GB/~5GB/~10GB/~6GB（turbo） |
| 11. 官方精度 | large-v3："10% to 20% reduction of errors compared to Whisper large-v2" 跨多语种；HF Open ASR Leaderboard 自报：large-v3 Mean WER **7.44**、AMI 15.95、RTFx 145.51；large-v3-turbo Mean WER **7.83**、AMI 16.13、RTFx 200.19。README 另有 Common Voice 15 / FLEURS 的分语种 WER/CER 图（large-v2 与 large-v3） |
| 12. 模态 | speech-to-text（ASR + X→En 翻译）。**turbo 不支持翻译**："turbo is not trained for translation tasks"，传 `--task translate` 会返回源语言 |
| 13. 源 | https://github.com/openai/whisper （README 表格、MIT、30s 窗）<br>https://raw.githubusercontent.com/openai/whisper/main/README.md<br>https://huggingface.co/openai/whisper-large-v3<br>https://huggingface.co/openai/whisper-large-v3-turbo<br>https://github.com/openai/whisper/releases （日期）<br>https://github.com/openai/whisper/discussions/2363 （turbo 发布说明）<br>https://arxiv.org/abs/2212.04356 |

**哪些尺寸真正能端侧跑**：tiny/base/small 是真正的手机/树莓派档（量化后 32–190 MB）；medium 在 PC/高端手机可行（q5_0 = 539 MB）；**large-v3-turbo q5_0 只有 574 MB，是目前"接近 large 精度 + 端侧可行"的甜点**；large-v3 全量 3.1 GB / q5_0 1.08 GB，只适合 PC/Mac 与车机级算力。

---

## 二、whisper.cpp（端侧主导运行时，非模型）

| 字段 | 内容 |
|---|---|
| 1. 全名 | whisper.cpp（ggml/GGUF C++ 端侧推理实现） |
| 2. 作者/归属 | Georgi Gerganov / ggml.ai。**注意：Gerganov 是保加利亚人，ggml.ai 已获美国资本（a16z）支持，做"国别归属"时需单独说明，不宜简单算作美国项目** |
| 3. 首发 | 2022-09（Whisper 发布后不久）；仓库现已迁至 `ggml-org/whisper.cpp` |
| 4. 参数量 | N/A（运行时） |
| 5. 架构 | ggml 张量库 + 纯 C/C++ 推理，零依赖，"Zero memory allocations at runtime"（内存前置预留） |
| 6. 压缩自 | N/A |
| 7. **量化选项 + 文件体积** | 支持整数量化，README 示例命令用 **q5_0**：`./build/bin/quantize models/ggml-base.en.bin models/ggml-base.en-q5_0.bin q5_0`。官方 HF 仓库 `ggerganov/whisper.cpp` 实际托管 **F16 / q5_1 / q5_0 / q8_0**（未托管 q4_0）：<br>· tiny: F16 77.7 MB / q5_1 **32.2 MB** / q8_0 43.5 MB<br>· base: F16 148 MB / q5_1 **59.7 MB** / q8_0 81.8 MB<br>· small: F16 488 MB / q5_1 **190 MB** / q8_0 264 MB<br>· medium: F16 1.53 GB / q5_0 **539 MB** / q8_0 823 MB<br>· large-v2: F16 3.09 GB / q5_0 1.08 GB / q8_0 1.66 GB<br>· large-v3: F16 3.1 GB / q5_0 **1.08 GB**（无 q8_0）<br>· **large-v3-turbo: F16 1.62 GB / q5_0 574 MB / q8_0 874 MB**<br>· Core ML encoder 包：tiny 15 MB / base 37.9 MB / small 163 MB / medium 568 MB / large-v3 1.18 GB / turbo 1.17 GB<br>惯例：tiny/base/small 用 q5_1，medium 及 large 系列用 q5_0 |
| 8. 音频窗 | 继承 Whisper 的 30 秒窗 |
| 9. 许可 | MIT |
| 10. **官方端侧性能** | **README 本身不含具体 benchmark 数字**，唯一定量声明是 Core ML："more than x3 faster compared with CPU-only execution"（Apple Silicon）。定量数据在官方 benchmark 汇总 issue #89（encoder-only encode 时间）：<br>· **MacBook M1 Pro**（macOS 13.0.1，NEON+BLAS，8 线程）：tiny 102 ms / base 220 ms / small 685 ms / medium 1928 ms / large 3350 ms（load 分别 71/96/233/603/1158 ms）<br>· 线程扩展（M1 Pro, small）：1 线程 2605 ms → 4 线程 884 ms → 8 线程 685 ms<br>· **Mac Mini M1**（4 线程）：tiny 194 / base 380 / small 1249 / medium 3980 / large 7979 ms<br>· **iPhone 13 Mini**（iOS 16.0，base，4 线程）：encode **1091 ms**（load 97 ms）<br>· **Raspberry Pi 4**（NEON，4 线程）：tiny **13839 ms** / base **30552 ms** —— 远慢于实时<br>**未见 M2 / M4 / M1 Max 条目。** |
| 11. 精度 | N/A（不改模型精度，量化会有轻微损失，仓库未给 WER 表） |
| 12. 模态 | speech-to-text 运行时 |
| 13. 源 | https://github.com/ggml-org/whisper.cpp （README）<br>https://raw.githubusercontent.com/ggml-org/whisper.cpp/master/README.md<br>https://raw.githubusercontent.com/ggml-org/whisper.cpp/master/models/README.md<br>https://huggingface.co/ggerganov/whisper.cpp/tree/main （逐文件体积）<br>https://github.com/ggml-org/whisper.cpp/issues/89 （benchmark 汇总） |

**后端支持**：Apple Silicon 为"first-class citizen"，走 ARM NEON / Accelerate / **Metal**（"On Apple Silicon, the inference runs fully on the GPU via Metal"）/ **Core ML**（encoder 可跑 Apple Neural Engine，需 `-DWHISPER_COREML=1` + `./models/generate-coreml-model.sh`；**首次运行慢，因为 ANE 要编译 Core ML 模型**）。另支持 CUDA/cuBLAS、Vulkan、HIP/ROCm、OpenVINO、**AMD Ryzen AI NPU (VitisAI)**、**昇腾 NPU (CANN)**、摩尔线程 MUSA、OpenBLAS。

**⚠️ 口径陷阱（已在仓库中证实）**：README 的 Memory usage 表**明确区分 Disk 与 Mem 两列**——tiny 磁盘 75 MiB / 内存 ~273 MB；base 142 MiB / ~388 MB；small 466 MiB / ~852 MB；medium 1.5 GiB / ~2.1 GB；large 2.9 GiB / ~3.9 GB。**运行时内存约为磁盘文件的 1.3–3.5 倍**，引用"文件只有 574 MB"时必须说明这不是内存占用。另外 `models/README.md` 的体积表用 MiB/GiB，HF 页面用 MB/GB，两者数字对不上是单位差异不是数据错。

---

## 三、Distil-Whisper（HuggingFace）

| 字段 | 内容 |
|---|---|
| 1. 全名/版本 | distil-large-v2 / distil-large-v3 / **distil-large-v3.5** / distil-medium.en / distil-small.en |
| 2. 公司 | Hugging Face（美国，纽约；法人 Hugging Face Inc.）。作者 Sanchit Gandhi、Patrick von Platen、**Alexander M. Rush（Cornell）** |
| 3. 首发 | 论文 arXiv:2311.00430 提交 **2023-11-01**；distil-large-v3 collection "Updated Mar 21, 2024" → **2024-03**；**distil-large-v3.5 collection "Updated Mar 25, 2025" → 2025-03**（模型卡未直接写发布日） |
| 4. 参数量 | distil-large-v2 **756M** / distil-large-v3 **756M** / distil-large-v3.5 **756M** / distil-medium.en **394M** / distil-small.en **166M**（对照 teacher large-v3 = 1550M） |
| 5. 架构 | encoder-decoder Transformer。**encoder 从 teacher 整体复制并冻结**（"entirely copied from the teacher to the student and frozen during training"），只缩减 decoder 层数：distil-medium.en 与 distil-large-v2 各留 **2 层 decoder**，**distil-small.en 留 4 层**；初始化取 teacher 首末/最大间隔层。训练目标 = KL 散度 + 伪标签交叉熵 |
| 6. **蒸馏自** | distil-large-v2 / v3 / v3.5 ← **OpenAI Whisper large-v3**（v3/v3.5 明确；v2 系 large-v2 时期）；**distil-small.en ← openai/whisper-small.en，训练目标由 Whisper large-v2 生成伪标签** |
| 7. 量化体积 | HF 侧未给量化文件表。whisper.cpp 侧有社区 ggml 转换（distil-large-v3 已被官方卡引用做 Mac M1 benchmark），具体量化体积**未披露** |
| 8. 音频窗 | 仍是 30 秒。**distil-large-v3 的核心改进正是修复了 v2 的窗口塌缩**：v2 训练时输入均值约 7 秒，有效上下文只有约 15 秒，超出后"the predictions for the distil-large-v2 model were largely inaccurate, particularly for the timestamp predictions"，导致 sequential 长音频转写常失败。v3 用四招修复：训练音频 pack 到 30 秒、冻结 decoder input embeddings、max target length 128→256、50% 样本加 prompt conditioning（支持 `condition_on_prev_tokens`，上下文窗到 448 token）。**chunked 模式官方推荐 chunk 25 秒（不是 30 秒）** |
| 9. 许可 | **MIT**（"inherits the MIT license from OpenAI's Whisper model"，可商用） |
| 10. **官方端侧/加速口径（重点核实）** | **⚠️ "6x faster / 49% smaller" 与论文原文不一致。**<br>· **论文摘要原文（arXiv:2311.00430）：**"The distilled model is **5.8 times faster with 51% fewer parameters**"，精度 "to within 1% WER on out-of-distribution test data in a zero-shot transfer setting"。**不是 6x，不是 49%。**<br>· GitHub/HF README 的营销口径：**"6 times faster, 49% smaller, and performs within 1% word error rate (WER) on out-of-distribution evaluation sets"**<br>· **测速硬件条件（论文 Appendix B.2，一手）：**"we evaluate the models in PyTorch using a **single A100 40GB GPU** in **float16** precision"；"we measure the total time taken to decode **256 samples** from each of the four OOD test sets over **batch sizes in the set {1, 4, 16}**"，开启 **Flash Attention 2**；短音频用 batch size 1，长音频 chunked 用 batch 4/16。**这是数据中心 GPU 数字，绝非端侧数字。**<br>· Rel. Latency 表（相对 large-v3 = 1.0）：distil-large-v3 **6.3x**、distil-large-v2 5.8x、distil-medium.en 6.8x、distil-small.en 5.6x<br>· **唯一的真端侧数字**：模型卡称在 **Mac M1** 上跑 whisper.cpp，distil-large-v3 比 whisper large-v3 "over 5x faster"，但官方自己标注为 **"a provisional benchmark"（临时基准）**；distil-small.en 在 Mac M1 whisper.cpp 上 "over 4x faster than large-v2"<br>· distil-large-v3.5：**"~1.5x faster" than Whisper-Large-v3-Turbo** 长音频（Rel. RTFx 1.46 vs turbo 1.0；长音频 RTFx 49.34 vs 33.81）——**硬件条件未披露**<br>· 其他：chunked 比 OpenAI sequential "up to 9x faster"；作为 speculative decoding 草稿模型给 large-v3 加速 "2 times faster"，显存增加约 8%，输出严格一致 |
| 11. 官方精度 | 汇总表（数据集在 README 中未点名，细节在论文 Table 16/17）：<br>· large-v3 (teacher)：Short 8.4 / Long-sequential 10.0 / Long-chunked 11.0<br>· **distil-large-v3：Short 9.7 / Long-seq 10.8 / Long-chunked 10.9**<br>· distil-large-v2：Short 10.1 / Long-seq 15.6 / Long-chunked 11.6<br>· distil-medium.en：Short 11.1 / Long 12.4；distil-small.en：Short 12.1 / Long 12.8<br>Open ASR Leaderboard 自报：distil-large-v3 Mean WER **7.52** / RTFx 214.42 / AMI 15.16；**distil-large-v3.5 Mean WER 7.21 / RTFx 202.03**；distil-small.en Mean WER 8.57 / AMI 16.16 / RTFx 331.89<br>distil-large-v3.5 分数据集短音频 WER：AMI 14.63 / GigaSpeech 9.84 / **LS Clean 2.37** / LS Other 5.04 / TEDLIUM 3.64 / Earnings22 11.29 / SPGISpeech 2.87；ID/OOD/Avg = 7.10/7.08/7.10。长音频（sequential）：tedlium-long 4.63 / meanwhile 6.79 / earnings21 10.6 / earnings22 14.19 / rev16 13.98，OOD avg 11.39<br>LibriSpeech validation-clean 单点：distil-large-v3 = **2.43** WER；distil-small.en = **3.43** WER |
| 12. 模态 | speech-to-text，**仅英文**（"currently only available for English speech recognition"；多语种官方建议改用 whisper-large-v3-turbo） |
| 13. 源 | https://github.com/huggingface/distil-whisper<br>https://huggingface.co/distil-whisper/distil-large-v3<br>https://huggingface.co/distil-whisper/distil-large-v3.5<br>https://huggingface.co/distil-whisper/distil-small.en<br>https://arxiv.org/abs/2311.00430 （摘要 5.8x/51%）<br>https://ar5iv.labs.arxiv.org/html/2311.00430 （Appendix B.2 硬件条件） |

**v3.5 相对 v3 的变化**（同为 756M）：训练数据 22k h → **超过 98,000 小时**（主要来自 Yodas YouTube，从约 196k h 按 10% WER 阈值过滤）；改用 "patient teacher" + SpecAugment，训练 80 epoch（v3 是 11 epoch）；prompt 拼接概率 50%→20%；batch size 256 → 4096 packed segments；加 BPE dropout。训练用 **64 张 H100**，Jean Zay 集群约 3 天。

---

## 四、OpenAI 是否有更新的开源权重语音模型？（2026-09 核实结果）

**结论：没有。截至 2026-09，Whisper large-v3-turbo（2024-09-30）仍是 OpenAI 最新的开放权重语音模型。**

- OpenAI 2025-03 发布的 **gpt-4o-transcribe / gpt-4o-mini-transcribe**（以及 gpt-4o-mini-tts）是 **API-only、闭源**，不提供权重，无法端侧部署。定价 $0.006/min 与 $0.003/min。新增流式支持（Whisper 是 batch-only）与噪声鲁棒性改进，走 Realtime API 与标准 audio API。之后 OpenAI 又把 batch / mini / diarized / streaming 转写拆成独立计费模型。
- openai/whisper GitHub 最新 release 是 **v20250625（2025-06-26）**，但那是代码包版本，**没有引入新模型权重**——最后一次引入新权重的 release 是 v20240930。
- gpt-oss 系列（2025-08 开放权重）是纯文本模型，不含语音。
- **注意**：openai.com 官方博客页在本次抓取中返回 403，上述"API-only"结论来自 WebSearch 聚合的多个 2026 年二手源 + openai/whisper releases 页面（一手）的交叉验证。做表时建议标注为"未见新开放权重语音模型（截至 2026-09 检索）"。
- 源：https://github.com/openai/whisper/releases ；https://northflank.com/blog/best-open-source-speech-to-text-stt-model-in-2026-benchmarks ；https://www.gladia.io/blog/best-whisper-alternatives-2026

---

## 五、NVIDIA NeMo ASR：Parakeet / Canary

### 5.1 Parakeet-TDT-0.6B-v2

| 字段 | 内容 |
|---|---|
| 1. 全名 | nvidia/parakeet-tdt-0.6b-v2（"Parakeet TDT 0.6B V2 (En)"） |
| 2. 公司 | NVIDIA（美国，圣克拉拉） |
| 3. 首发 | **2025-05-01**（模型卡 Release Date 05/01/2025） |
| 4. 参数量 | **600M**（"This model has 600 million model parameters"） |
| 5. 架构 | **FastConformer 编码器 + TDT（Token-and-Duration Transducer）解码器**，XL 变体，full attention。带词级时间戳、自动标点大小写 |
| 6. 压缩自 | 否。从 wav2vec 式 SSL checkpoint（LibriLight）初始化，64×A100 训 150k step，再用 4×A100 做 2500 step 二阶段微调 |
| 7. 量化体积 | 官方卡未给量化文件。最低要求 **"At least 2GB RAM for model to load"**。量化体积**未披露** |
| 8. 音频窗 | **单次可处理最长 24 分钟音频**（"audio segments up to 24 minutes in a single pass"）—— **相对 Whisper 30 秒窗是根本性优势**。输入 16 kHz 单声道 .wav/.flac |
| 9. 许可 | **CC-BY-4.0**，"ready for commercial/non-commercial use" |
| 10. **官方性能（口径警告）** | 卡上称 "an **RTFx of 3380** on the HF-Open-ASR leaderboard with a **batch size of 128**"，并自加免责："RTFx Performance may vary depending on dataset audio duration and batch size"。**⚠️ 模型卡本身没有写这个 RTFx 用的是哪块 GPU。** 卡上列的推理测试硬件是 **A10 / A100 / A30 / H100 / L4 / L40 / T4 / V100**，支持架构 Ampere / Blackwell / Hopper / Volta。**Open ASR Leaderboard 官方仓库说明：所有短音频英文与多语评测统一在 Hugging Face Jobs 的 `h200` flavor 上跑——"h200 Nvidia H200 23 vCPU 256 GB 3000 GB 1x H200 (141 GB)"，为的是"every run executes a Docker image on the same hardware"。即 RTFx 3380 是 141 GB 显存的 H200 数据中心卡 + batch 128 的数字，与 Jetson/手机端侧性能毫无关系。** 官方卡**完全没有提到 Jetson 或 edge 部署**；OS 仅支持 Linux，运行时 NeMo 2.2 |
| 11. 官方精度 | Open ASR Leaderboard（transducer greedy，无外部 LM）**Avg WER 6.05**：AMI 11.16 / Earnings-22 11.15 / GigaSpeech 9.74 / **LS test-clean 1.69** / LS test-other 3.19 / SPGI 2.17 / TEDLIUM-v3 3.38 / VoxPopuli 5.95。RTFx 3386.02。<br>噪声鲁棒性（MUSAN）：SNR 10 → 6.95；SNR 5 → 8.23；SNR 0 → 11.88；SNR −5 → 20.26。电话 μ-law 8kHz → 6.32（相对劣化 −4.10%） |
| 12. 模态 | speech-to-text（仅英文） |
| 13. 源 | https://huggingface.co/nvidia/parakeet-tdt-0.6b-v2 ；https://github.com/huggingface/open_asr_leaderboard （H200 评测硬件） |

**训练数据**：Granary 数据集，约 **120,000 小时**英文语音 = 约 10,000 h 人工转写（NeMo ASR Set 3.0：LibriSpeech 960h、Fisher、National Speech Corpus P1、VCTK、VoxPopuli EN、Europarl-ASR EN、MLS English 2000h 子集、Common Voice v7.0、AMI）+ **110,000 h 伪标签**（YouTube-Commons、YODAS、LibriLight）。

### 5.2 Parakeet-TDT-0.6B-v3

| 字段 | 内容 |
|---|---|
| 1–2 | nvidia/parakeet-tdt-0.6b-v3；NVIDIA |
| 3. 首发 | **2025-08-14** |
| 4. 参数量 | **600M** |
| 5. 架构 | FastConformer + TDT（同 v2）。**语言自动检测，无需 prompt** |
| 6. 压缩自 | 否 |
| 7. 量化体积 | **有 q8_0 GGUF 量化 checkpoint**，配套 **NeMo-Speech.cpp**——卡上描述为 "a lightweight native C++ runtime for local inference"。**这是 Parakeet 唯一明确的端侧路径**，但**具体量化文件体积未披露**。最低 ~2GB RAM 加载 |
| 8. 音频窗 | full attention 下单次 **最长 24 分钟（在 A100 80GB 上）**；改用 local attention 可扩展到 **约 3 小时** |
| 9. 许可 | **CC-BY-4.0**（"permissive CC BY 4.0 license"） |
| 10. 官方性能 | **RTFx 3332.74**，来源标注为 hf-audio Open ASR Leaderboard。**⚠️ 模型卡未写该 RTFx 的 GPU 与 batch size**（同上，Leaderboard 侧为 1×H200）。列出的推理测试硬件：A10/A100/A30/H100/L4/L40/T4/V100。训练用 128×A100（150k step）+ 4×A100 二阶段。**卡上无任何 Jetson/edge 引用** |
| 11. 官方精度 | Open ASR Leaderboard 自报 **Avg 6.34%**：AMI 11.31 / Earnings-22 11.42 / GigaSpeech 9.59 / LS clean **1.93** / LS other 3.59 / SPGI 3.97 / TEDLIUM-v3 2.75 / VoxPopuli 6.14（Leaderboard 侧数据 Mean 6.32、AMI 11.39）。<br>多语（greedy Transducer，无外部 LM）：**FLEURS 11.97% / MLS 7.83% / CoVoST 11.98%**；FLEURS 区间 3.00%（意大利语）～24.03%（斯洛文尼亚语），英文 FLEURS 4.85%。<br>MUSAN 噪声：6.34 → 7.12（SNR10）→ 8.23（SNR5）→ 11.66（SNR0）→ 19.88（SNR−5） |
| 12. 模态 | speech-to-text，**25 种欧洲语言**（bg, hr, cs, da, nl, en, et, fi, fr, de, el, hu, it, lv, lt, mt, pl, pt, ro, sk, sl, es, sv, ru, uk） |
| 13. 源 | https://huggingface.co/nvidia/parakeet-tdt-0.6b-v3 |

### 5.3 Canary-1B-v2

| 字段 | 内容 |
|---|---|
| 1–2 | nvidia/canary-1b-v2（"Canary-1b-v2"）；NVIDIA |
| 3. 首发 | **2025-08-14** |
| 4. 参数量 | **978M**（"totaling 978 million parameters"；HF sidebar 取整为 1.0B） |
| 5. 架构 | **Attention Encoder-Decoder：FastConformer 编码器（32 层）+ Transformer 解码器（8 层）**，共享 SentencePiece 词表 16,384 token。用 `<source language>` / `<target language>` task token 控制解码 |
| 6. 压缩自 | 否 |
| 7. 量化体积 | 未披露。最低 **"At least 6GB RAM for model to load"**。可选省内存做法：若不需要时间戳，可重新打包 `.nemo` 去掉辅助 CTC 时间戳模型 |
| 8. 音频窗 | 卡上未给最大时长。长音频用**动态分块，块间 1 秒重叠**，在单文件或 batch_size=1 且音频超过 **40 秒**时自动触发 |
| 9. 许可 | **CC-BY-4.0**，可商用 |
| 10. 官方性能 | **RTFx 749**。**⚠️ 模型卡未注明该数字的 GPU。** 列出的推理测试硬件：A10/A100/A30/A5000/H100/L4/L40，架构 Ampere/Blackwell/Hopper。**无任何 edge 部署说明**；OS 仅 Linux；目前无 Inference Provider 托管 |
| 11. 官方精度 | Open ASR Leaderboard **Mean WER 7.15**：AMI 16.01 / GigaSpeech 10.82 / LS Clean 2.18 / LS Other 3.56 / Earnings22 11.79 / SPGISpeech 2.28 / TEDLIUM 4.29 / VoxPopuli 6.25 |
| 12. 模态 | **speech-to-text + 语音翻译**：25 种欧洲语言 ASR；翻译覆盖 英→24 语 与 24 语→英 |
| 13. 源 | https://huggingface.co/nvidia/canary-1b-v2 |

### 5.4 Canary-Qwen-2.5B

| 字段 | 内容 |
|---|---|
| 1–2 | NVIDIA NeMo Canary-Qwen-2.5B；NVIDIA |
| 3. 首发 | **2025-07-17** |
| 4. 参数量 | **2.5B**（HF 元数据标 3B params，BF16） |
| 5. 架构 | **SALM（Speech-Augmented Language Model）**：FastConformer 编码器产出音频 embedding → 线性投影映射进 LLM embedding 空间 → 与文本 token embedding 拼接 → Transformer 解码器；LLM 侧挂 **LoRA adapter** |
| 6. **组合自** | **nvidia/canary-1b-flash（语音编码器）+ Qwen/Qwen3-1.7B（LLM，tokenizer 也继承自它）**。model tree 根 base model = Qwen3-1.7B-Base。⚠️ **含中国 Qwen 血统，做"美国模型"归类时必须标注** |
| 7. 量化体积 | 未披露 |
| 8. 音频窗 | 训练最长音频 **40 秒**，最大序列 1024 token |
| 9. 许可 | **CC-BY-4.0** |
| 10. 官方性能 | **RTFx 418**（leaderboard 元数据 418.28）。**⚠️ 模型卡未写该 RTFx 的具体 GPU。** 测试硬件列的是 **A6000 / A100 / RTX 5090**；训练用 32×A100 80GB、90k step（LLM 冻结）。**支持架构从 Pascal 到 Blackwell，并明确列出 Jetson**——是 NVIDIA 语音模型中唯一明写 Jetson 的，但**没有给任何 Jetson 上的实测性能数字** |
| 11. 官方精度 | Open ASR Leaderboard（whisper-normalizer 0.1.12，greedy，不带 PnC）**Mean WER 5.63**：AMI 10.18 / GigaSpeech 9.41 / **LS Clean 1.60** / LS Other 3.10 / Earnings22 10.42 / SPGISpeech 1.90 / TED-LIUM 2.72 / VoxPopuli 5.66。卡上只称 "state-of-the art performance on multiple English speech benchmarks"，未声明排名。<br>公平性评测：男声 WER 16.71 高于女声 13.85 |
| 12. 模态 | **双模态双模式**：ASR 模式转写英文（带标点大小写），但"does not retain any LLM-specific skills such as reasoning"；LLM 模式（`model.llm.disable_adapter()`）恢复 Qwen 能力做转写后处理（摘要、问答）。**⚠️ 关键限制：LLM 模式下只看文本——卡上明说 "does not 'understand' the raw audio anymore - only its transcript"，因此不是真正的端到端音频理解模型** |
| 13. 源 | https://huggingface.co/nvidia/canary-qwen-2.5b |

**训练数据**：约 234k 小时 / 约 40M 语音-文本对，主要来自 Granary（YouTube-Commons、YODAS2、LibriLight）。AMI 过采样到约 15%，导致输出偏向逐字保留口语不流利现象。仅英文（编码器预训练含少量德/法/西，但多语使用不可靠）。

---

## 六、Kyutai（按要求 SKIP，仅备注）

Kyutai 是**法国**非营利实验室（巴黎），Moshi / Mimi / Hibiki 全部法国血统。**未发现任何美国相关的 Moshi 端侧变体**——Moshi 的 Rust/MLX/Candle 端侧实现均由 Kyutai 自己维护。建议在产业图谱中归入"欧洲"而非美国。本次调研未展开抓取。

---

## 七、其他"真美国 + 真端侧"语音模型

### 7.1 ⭐ Moonshine / Moonshine v2（Moonshine AI，前 Useful Sensors）—— 本次调研最重要的遗漏补充

这是**最纯粹的美国端侧优先 ASR 模型**，作者含 Pete Warden（TensorFlow Lite Micro 创始人），且**专门为了绕开 Whisper 的 30 秒窗而设计**。强烈建议纳入对比表。

**Moonshine v1（2024-10）**

| 字段 | 内容 |
|---|---|
| 1. 全名 | Moonshine Tiny / Moonshine Base |
| 2. 公司 | Moonshine AI（原 Useful Sensors，美国加州）。作者：Nat Jeffries, Evan King, Manjunath Kudlur, Guy Nicholson, James Wang, **Pete Warden** |
| 3. 首发 | **2024-10**（arXiv:2410.15608 提交 2024-10-21） |
| 4. 参数量 | **Tiny 27.1M / Base 61.5M**（对照 whisper tiny.en 37.8M、base.en 72.6M） |
| 5. 架构 | encoder-decoder Transformer，**RoPE 取代绝对位置编码**；encoder dim 288(Tiny)/416(Base)，6/8 层 encoder + 6/8 层 decoder，8 attention heads；encoder FFN 用 GELU，**decoder FFN 用 SwiGLU** |
| 6. 蒸馏自 | 否，从头训练（但训练用 Whisper large-v3 给网络音频打标/过滤） |
| 7. 量化体积 | 官方 HF 权重 F32；量化体积见 v2 章节 |
| 8. **音频窗（核心卖点）** | **不做 30 秒零填充**："trained on speech segments of various lengths, but without using zero-padding"，计算量随实际音频长度缩放。论文实测 Whisper 强行截断/前后缀补位的代价：LibriSpeech test-clean beam 5 下 whisper tiny.en 正常 5.21% WER，加位置嵌入前缀变 **107.38%**，加后缀变 18.45% —— 这就是 30 秒固定窗的硬约束证据。训练样本组织在 4–30 秒区间 |
| 9. 许可 | **MIT** |
| 10. 官方性能 | 摘要口径：对 Whisper tiny-en，转写 10 秒语音有 **"a 5x reduction in compute requirements"**（注意是**计算量**下降，不是墙钟加速；摘要未给硬件）。论文正文：GPU 对比在 **H100** 上做，且只用 30 秒以内片段以避免对 Whisper 不公；变长编码的 GFLOPS 分析给出 "a **35x** speed-up in the best case, and nearly a **5x** speed-up overall"；结论段称按输入时长缩放最多带来 3x 延迟下降。**端侧硬件证据**：在 Caption Box 产品的 "a low-cost ARM-based processor" 上，whisper tiny.en 存在 **"a firm lower latency bound of 500 milliseconds"**，与音频长度无关。训练用 **32×H100**（Lambda Labs），BF16，250k step，global batch 1024 |
| 11. 官方精度 | Open ASR Leaderboard 8 数据集（greedy）：**Moonshine Base avg 10.07 vs whisper base.en 10.32**；**Moonshine Tiny avg 12.66 vs whisper tiny.en 12.81**。分项（whisper / moonshine）Base：AMI 21.13/17.79、Earnings22 15.09/17.65、GigaSpeech 12.83/12.19、LS Clean 4.25/**3.23**、LS Other 10.35/**8.18**、SPGI 4.26/5.46、TEDLium 4.87/5.22、VoxPopuli 9.76/10.81。HF 卡自报 Mean WER 9.99 / AMI 17.49 / **RTFx 565.97** |
| 12. 模态 | speech-to-text（v1 英文） |
| 13. 源 | https://arxiv.org/abs/2410.15608 ；https://arxiv.org/html/2410.15608v2 ；https://huggingface.co/UsefulSensors/moonshine-base |

**训练数据**：约 200K 小时 = 约 90K h 开源语料（Common Voice 16.1、AMI、GigaSpeech、LibriSpeech、English MLS、People's Speech）+ 超 100K h 自采网络音频（Whisper large-v3 打标 + 归一化 Levenshtein 阈值过滤 + 平均对数概率阈值筛幻觉）。

**Moonshine v2（2026-02）—— 全新一代，流式**

| 字段 | 内容 |
|---|---|
| 1. 全名 | **Moonshine v2: Ergodic Streaming Encoder ASR**；模型 Tiny Streaming / Small Streaming / Medium Streaming |
| 3. 首发 | **2026-02-12**（arXiv:2602.12241 提交 2026-02-12） |
| 4. 参数量 | **Tiny 33.57M**（pre 2.08 / enc 7.39 / adapter 1.31 / dec 22.80）<br>**Small 123.36M**（7.74 / 43.49 / 2.86 / 69.27）<br>**Medium 244.93M**（11.86 / 93.66 / 3.64 / 135.77） |
| 5. 架构 | **滑动窗自注意力流式编码器（"ergodic"，encoder 无位置编码）** + 因果 decoder（RoPE + SwiGLU）。窗口 (w_left, w_right)：首末各两层用 **(16,4)**，中间层全部 **(16,0)（严格因果）**。每帧 20 ms（50 Hz 特征）→ **算法前瞻仅 80 ms**，完全定稿态最多需 16 帧（约 320 ms）未来音频。前端：原始音频 → 80 样本（5 ms @16kHz）非重叠窗 → 逐帧 CMVN → asinh 非线性 → 两层因果 stride-2 卷积 |
| 6. 蒸馏自 | 否 |
| 7. **量化** | **默认全量 8-bit 权重 + MatMul 等重算子 8-bit 计算**，纯 post-training（OnnxRuntime + Onnx Shrink Ray）。前端卷积因输入是 16-bit 音频，必须保持 ≥B16 float。**采用 per-output-channel scale 而非 per-tensor**（Tiny Streaming 上最大通道是最小通道的 17 倍）。量化精度影响（LibriSpeech test-clean WER，per-tensor → per-channel）：**Tiny 7.57% → 4.83%；Small 3.03% → 2.61%；Medium 2.37% → 2.17%**，约 90% 改善来自前端，代价是下载体积 +0.5%。**逐模型 MB 体积未披露**（文档只给相对量）。量化包按日期目录分发（当前 `quantized_26_08_21`） |
| 8. 音频窗 | **无 30 秒固定窗**，滑动窗流式，TTFT 与音频长度基本无关（v1 的全注意力 encoder TTFT 随音频长度线性上升，v2 编码延迟近似常数） |
| 9. 许可 | **MIT**（代码 + 各语言各尺寸模型默认 MIT）。**例外：非英语的 legacy 非流式模型走非商用 "Moonshine Community License"**，清单在 LICENSE 文件 |
| 10. **官方端侧性能（★ 罕见的真端侧口径）** | **硬件明确：Apple M3 MacBook CPU**，Moonshine 用作者的 C++ + ONNX Runtime（CPU）实现，Whisper 用 faster-whisper。**Response latency = VAD 检测语音结束到返回文本**（在线指标，非吞吐）：<br>· Moonshine Tiny (v1) **27 ms**，compute load 5.91%<br>· Moonshine Base (v1) **44 ms**，7.34%<br>· **Moonshine v2 Tiny 50 ms，8.03%**<br>· **Moonshine v2 Small 148 ms，17.97%**<br>· **Moonshine v2 Medium 258 ms，28.95%**<br>· Whisper Tiny 289 ms，8.46%<br>· Whisper Base 553 ms，16.19%<br>· Whisper Small 1940 ms，56.84%<br>· **Whisper Large v3 11286 ms，compute load 330.65%（>100% 即慢于实时）**<br>加速比：v2 Tiny 比 Whisper Tiny 快 **5.8x**；v2 Small 比 Whisper Small 快 **13.1x**；**v2 Medium 比 Whisper Large v3 快 43.7x**。另有分析性估算：Arm **Cortex-A55 @2.31 GHz 约 74 GOPS**，目标算力区间 0.1–1 TOPS。**论文明确无树莓派/手机实测数据** |
| 11. 官方精度 | Open ASR Leaderboard 8 数据集平均：**Tiny 12.01 / Small 7.84 / Medium 6.65**。分项 Tiny/Small/Medium：AMI 19.03/12.54/10.68、Earnings-22 20.27/13.53/11.90、GigaSpeech 13.90/10.41/9.46、**LS clean 4.49/2.49/2.08**、LS other 12.09/6.78/5.00、SPGI 6.16/3.19/2.58、TED-LIUM 6.12/3.77/2.99、VoxPopuli 14.02/9.98/8.54。<br>量化后 LS test-clean（论文 float / 复现 float / 出货量化版）：Tiny 4.49/4.52/**4.83**；Small 2.49/2.55/**2.61**；Medium 2.08/2.16/**2.17**。<br>多语（量化版，400 clip 抽样）：阿拉伯语 Tiny 15.5% WER；德语 Small/Tiny 7.5%/12.0%；西班牙语 4.9%/6.2%；日语 Small/Tiny **CER** 17.2%/19.7%；普通话 Tiny CER 16.1%；他加禄语 14.9%；越南语 9.4%。（韩语 Tiny 6.46%、乌克兰语 Base 14.55% 来自旧的全量 FLEURS 评测，**官方警告与 400-clip 行不可比**）<br>论文声称"accuracy on-par with models 6x their size"，Figure 4 与 canary-qwen-2.5b、parakeet-tdt-0.6b-v2、whisper-large-v3、whisper-large-v3-turbo 同图比较，称 Moonshine 与 NVIDIA 在同一 Pareto 前沿且高于 OpenAI |
| 12. 模态 | **STT**（英、西、中、日、韩、越、乌、阿）+ **TTS**（16 种语言，含约 4 秒音频的 zero-shot 声音克隆）+ VAD + 语音 agent |
| 13. 源 | https://arxiv.org/abs/2602.12241 ；https://arxiv.org/html/2602.12241v1 ；https://github.com/moonshine-ai/moonshine ；https://moonshine-voice.readthedocs.io/en/latest/models/accuracy/ ；https://moonshine-voice.readthedocs.io/en/latest/models/quantization/ ；https://huggingface.co/moonshine-ai |

**Moonshine Micro（MCU 级，极端端侧）**：目标平台 **Raspberry Pi RP2350（零售 80 美分）**，MCU/DSP 级。组件：TinyVadCNN（VAD，flash ~89 KiB / SRAM ~36 KiB / ~25 MMAC/s）、**SpellingCNN（STT，flash ~1.3 MiB / SRAM ~346 KiB / ~36 MMAC/s）**、神经 diphone 合成器（TTS，voice pack ~1.8 MiB / SRAM ~340 KiB）。整链路 flash ~3.6 MiB，SRAM 预留 ~468 KiB，**三段时分复用同一个约 384 KiB TFLM arena，内存不叠加**，官方称可在 **470 KB RAM** 内运行；分类+朗读端到端 0.7–1.0 s。跑 TensorFlow Lite Micro。代码与 `models/` 为 MIT。**未给任何 WER 数字。** 源：https://github.com/moonshine-ai/moonshine/blob/main/micro/README.md

平台覆盖：Python、JavaScript/WASM、iOS、Android、macOS、Linux、Windows、Raspberry Pi。`pip install moonshine-voice`。

### 7.2 Picovoice（Cheetah / Leopard / Orca）

| 字段 | 内容 |
|---|---|
| 1. 全名 | Leopard（批处理 STT）、Cheetah（流式 STT）、Orca（流式 TTS）、Porcupine（唤醒词）、Rhino（NLU） |
| 2. 公司 | **Picovoice Inc. —— ⚠️ 加拿大公司（通常记为温哥华 BC）。本次抓取中官网 company/careers/privacy 页面均未给出城市国别（careers 页只有 "© 2019-2026 Picovoice Inc."），HQ 属"未一手验证"。做国别归类时请标注"加拿大，非美国"** |
| 3. 首发 | Cheetah / Leopard 约 2022（Hackster 报道页本次抓取 403，日期未一手验证） |
| 4. 参数量 | **未披露**（闭源专有） |
| 5. 架构 | **未披露**，仅称"深度学习"，端到端自研训练框架 + 自研推理引擎 |
| 6. 压缩自 | 未披露 |
| 7. 体积 | 发布时报道称两个模型均 **<20 MB**（二手源，Hackster；官方页未直接验证）。官方称适合资源受限设备但**不含 MCU** |
| 8. 音频窗 | 无 30 秒约束（流式引擎） |
| 9. 许可 | **闭源商业授权**（免费额度 + Picovoice Console 定制），非开放权重 |
| 10. 官方性能 | 官方 STT benchmark **不用 RTF，用 "Core-Hour"**（处理 1 小时音频所需 CPU 核小时，越低越好，云端引擎不计）。**明确声明："Whisper Tiny requires 6x more resources or takes 6x more time compared to Picovoice Leopard"**。测试条件：**Ubuntu 22.04，AMD Ryzen 9 5900X（12 核 @3.70 GHz），64 GB RAM，NVMe，10 核并行，全量 LibriSpeech test-clean**。⚠️ 这是桌面 x86 CPU，不是手机/MCU。**逐引擎 Core-Hour 具体数值在页面图表中，本次抓取未取到数字** |
| 11. 精度 | 官方 benchmark 数据集：英文 = LibriSpeech test-clean / test-other、Common Voice test、TED-LIUM test；法/德/西/意 = MLS test、Common Voice test、VoxPopuli test；葡 = MLS + Common Voice。**具体 WER 数值以图表呈现，本次抓取未取到**。发布时报道称 Cheetah 14.34% WER、Leopard 11% WER（二手源）。Cheetah 另有独立"实时转写 benchmark"，对手为 Azure Real-Time STT、Amazon Transcribe Streaming、Google Streaming ASR、**Moonshine Streaming**、Vosk Streaming、**Whisper.cpp Streaming** |
| 12. 模态 | STT（Leopard 批 / Cheetah 流）+ TTS（Orca，面向 LLM 流式输出，宣称零延迟语音助手）+ 唤醒词 + NLU + 降噪 + 说话人分离/识别 + VAD |
| 13. 源 | https://picovoice.ai/docs/benchmark/stt/ ；https://picovoice.ai/docs/ ；https://github.com/Picovoice/speech-to-text-benchmark （benchmark 代码 Apache 2.0） |

Leopard 语言：英、法、德、意、日、韩、葡、西。支持自定义词表（可带 IPA 发音）与词概率提升。

### 7.3 Cartesia（Sonic TTS / Ink STT）

| 字段 | 内容 |
|---|---|
| 1. 全名 | **Sonic**（TTS）、**Ink**（流式 STT）、Managed Agents |
| 2. 公司 | Cartesia（美国，旧金山；创始团队来自 Stanford，Mamba/SSM 作者系） |
| 3. 首发 | 官网**未给任何发布日期** |
| 4. 参数量 | **未披露** |
| 5. 架构 | **State Space Model（SSM）**——官网："Built on State Space Models (SSMs), a new primitive for low latency, long-context reasoning, and greater efficiency at scale"，研究页明确列 **SSM、Mamba、H-Nets** |
| 6. 压缩自 | 未披露 |
| 7. 量化体积 | **未披露**。唯一相关表述是内存特性："Constant memory usage" / "Run large models on small devices without hogging memory" |
| 8. 上下文 | 未披露（SSM 天然常数内存流式） |
| 9. 许可 | **Sonic / Ink 权重不开放**，商业 API。⚠️ **官方开源仓库 `cartesia-ai/edge`（Apache-2.0）里没有语音模型**——里面是 **Llamba-1B/3B/8B**（2025-02-24，从 Llama-3.2-1B/3B-Instruct 与 Llama-3.1-8B-Instruct 蒸馏）和 **Rene-v0.1-1.3b**（2024-08-27），都是语言模型。包含 `cartesia-pytorch` / `cartesia-metal`（Apple Silicon 笔电与移动端自定义 Metal kernel）/ `cartesia-mlx`。**不要把 Cartesia 的开源 edge 仓库误当成 Sonic 的端侧开源版** |
| 10. 官方端侧性能 | **完全未披露具体数字**。官网只有定性表述："fast enough for live conversation"、"fastest"、"a first-of-its-kind low-latency state space model stack"。**无毫秒数、无硬件名、无芯片名**。列出 cloud / on-premise / on-device 三种部署，宣称 "The same models and agents across cloud, on-premise, and on-device" 与 "own your inference and deploy and run models on custom hardware"，但**不点名任何 SoC/加速器** |
| 11. 精度 | 引用第三方 Artificial Analysis "Ranked #1" on Speech Arena / Speech to Text leaderboard，**无日期无版本，无法验证** |
| 12. 模态 | TTS + STT + voice agent |
| 13. 源 | https://cartesia.ai/ ；https://cartesia.ai/on-device ；https://github.com/cartesia-ai/edge |

**结论：Cartesia 是美国 + 架构上确实为端侧设计（SSM 常数内存），但语音模型规格几乎全部未披露、权重不开放，做技术对比表时大部分字段只能填"未披露"。**

### 7.4 Deepgram（美国，波士顿）—— 无端侧模型

自托管文档只覆盖 **Docker/Podman、Kubernetes、Amazon SageMaker** 三种服务器部署，架构是 API + Engine（"manages GPU devices"）+ 可选 License Proxy，需 Enterprise Plan。**文档完全不涉及 on-device / embedded / edge**，也未在该页点名 Nova-3 / Aura。SageMaker 路径可 "fully airgapped" 但仍是云上实例。**具体 GPU 型号与最低显存未披露。** 结论：**Deepgram 属"云 + 私有化服务器"，不属于端侧模型，建议在图谱中明确排除或标为"仅私有化部署，非端侧"。** 源：https://developers.deepgram.com/docs/self-hosted-introduction

### 7.5 AssemblyAI（美国，旧金山）—— 纯云

无开放权重、无端侧 SDK。本次未展开抓取。建议标为"纯云 API"。

### 7.6 Silero VAD

按要求跳过（Silero Team 俄罗斯血统）。

---

## 八、四个"口径陷阱"的一手证据汇总（建议直接放进图谱注释）

**陷阱 1 — Whisper 的 30 秒固定窗**
- 一手原文（HF 模型卡）："Whisper has a receptive field of 30-seconds."
- 一手原文（GitHub README）：长音频靠 "sliding 30-second window, performing autoregressive sequence-to-sequence predictions on each window"；低阶 API 示例要把音频 pad 或 trim 到 30 秒再转 log-Mel。
- **后果**：任何"Whisper 延迟 X ms"的说法都必须说明是 30 秒块的处理时间还是端到端首字延迟。Moonshine 论文给了硬证据：在低成本 ARM 处理器上，whisper tiny.en 有 **500 ms 的延迟下界，且与音频长度无关**（因为无论说 1 秒还是 30 秒，encoder 都要算满 30 秒）。另一硬证据：LibriSpeech test-clean 上 whisper tiny.en 正常 5.21% WER，一旦给位置嵌入加前缀（模拟非 30 秒对齐输入）飙到 **107.38%**。
- **长音频两种切法结果不同**：HF 官方说 sequential（滑窗缓冲）比 chunked 在批量长文件上 "up to 0.5% WER more accurate"，延迟相当；large-v3 最优 chunk = 30 秒，但 **distil-large-v3 最优 chunk = 25 秒**。distil-whisper 的 chunked 比 OpenAI sequential "up to 9x faster" —— 同一个模型换切法就能差 9 倍，速度数字不说切法等于没说。

**陷阱 2 — RTFx / 加速比离开硬件无意义，NVIDIA 的 RTFx 全是数据中心数字**
- **Open ASR Leaderboard 的统一评测硬件是 1×H200（141 GB）**，仓库 README 原文列出 flavor："h200  Nvidia H200  23 vCPU  256 GB  3000 GB  1x H200 (141 GB)"，目的是 "every run executes a Docker image on the same hardware, to minimize environment and driver differences"（长音频评测尚未迁移到该统一环境）。
- 因此 **parakeet-tdt-0.6b-v2 的 RTFx 3380 / v3 的 3332.74 / canary-1b-v2 的 749 / canary-qwen 的 418，全部是 141 GB 显存的 H200 数据中心卡上的数字，与 Jetson、手机、PC 无任何关系。** NVIDIA 自己的模型卡甚至**都没写 RTFx 用的哪块 GPU**，只笼统列 "A10/A100/A30/H100/L4/L40/T4/V100" 作为测试硬件，并且 parakeet 卡自加免责 "RTFx Performance may vary depending on dataset audio duration and batch size"。parakeet v2 的 RTFx 明确是 **batch size 128**（端侧真实场景 batch=1）。
- Whisper README 的 "Relative speed"（tiny ~10x … turbo ~8x）同理，**是 A100 上转写英文测的**，README 自己写 "benchmarked on an A100 transcribing English"。
- **四个 NVIDIA 模型中只有 canary-qwen-2.5b 的支持架构列表里明写了 Jetson，且没有给任何 Jetson 上的实测数字。** parakeet v2/v3 与 canary-1b-v2 的模型卡**完全没提 Jetson 或 edge**。parakeet v3 唯一的端侧路径是 **NeMo-Speech.cpp + q8_0 GGUF**（"a lightweight native C++ runtime for local inference"），但体积和性能均未披露。
- **反例（值得表扬的口径）**：Moonshine v2 论文把硬件写死为 **Apple M3 MacBook CPU**，指标定义为 "VAD 检测语音结束 → 返回文本" 的 response latency，并同时给 compute load（RTF 的倒数），>100% 即慢于实时（Whisper Large v3 = 330.65%）。这是本次调研中**唯一一组真正的消费级端侧实测数字**。

**陷阱 3 — whisper.cpp 的体积是磁盘上的量化文件，不是运行内存**
- whisper.cpp README 的 Memory usage 表**自带 Disk 和 Mem 两列**：tiny 75 MiB / ~273 MB；base 142 MiB / ~388 MB；small 466 MiB / ~852 MB；medium 1.5 GiB / ~2.1 GB；large 2.9 GiB / ~3.9 GB。**运行时内存是磁盘文件的 1.3–3.5 倍。**
- 另外还有单位陷阱：`models/README.md` 用 **MiB/GiB**（tiny 75 MiB、large-v3-turbo-q5_0 547 MiB），HF 文件列表用 **MB/GB**（tiny 77.7 MB、turbo-q5_0 574 MB），是同一个文件的不同单位，别当成两组数据。
- 还有一层隐藏成本：**Core ML encoder 是额外的一份文件**（large-v3 的 mlmodelc.zip 就有 1.18 GB），启用 ANE 时磁盘占用要把 ggml 文件和 mlmodelc 加在一起；而且 "The first run on a device is slow, since the ANE service compiles the Core ML model"，**首帧延迟不能拿冷启动数据报**。
- whisper.cpp 声称 "Zero memory allocations at runtime"，即内存全部前置预留 —— 所以 Mem 列是**峰值即常驻**，不是平均值。

**陷阱 4 — distil-whisper 的 "6x faster / 49% smaller" 与论文原文不符，且是 A100 数字**
- **论文摘要原文是 "5.8 times faster with 51% fewer parameters"**，README/模型卡的营销口径写成 "6 times faster, 49% smaller"。两者都指同一件事，但**引用时应以论文的 5.8x / 51% 为准，并注明 README 口径为 6x / 49%**。
- **测速条件（论文 Appendix B.2 一手原文）**："we evaluate the models in PyTorch using a **single A100 40GB GPU** in **float16** precision"；"we measure the total time taken to decode **256 samples** from each of the four OOD test sets over **batch sizes in the set {1, 4, 16}**"；开启 **Flash Attention 2**；短音频 batch=1、长音频 chunked batch=4/16；基准点是 Whisper large-v2。
- **模型卡的 Rel. Latency 表（distil-large-v3 = 6.3x）完全没有标注硬件、batch、精度** —— 这一栏在模型卡上就是裸数字。
- distil-large-v3.5 的 "~1.5x faster than large-v3-turbo"（RTFx 1.46 vs 1.0）**同样未披露硬件**。
- **唯一的端侧数字**：模型卡称在 **Mac M1** 上跑 whisper.cpp，distil-large-v3 比 large-v3 "over 5x faster"、长音频 WER 差距在 0.8% 以内，但官方自己标注 **"a provisional benchmark"（临时基准）** —— 引用时必须带上这个限定词。

**附加陷阱 5 — 许可与国别的两处坑**
- **Whisper 的 license 在一手源之间不一致**：github.com/openai/whisper README 明写 MIT，whisper-large-v3-turbo 的 HF 卡 license 字段是 `mit`，但 **openai/whisper-large-v3 的 HF 卡 license 字段是 `apache-2.0`**。建议以 repo 的 MIT 为准并加注。
- **国别**：whisper.cpp 作者 Georgi Gerganov 是保加利亚人（ggml.ai 有美国资本），不宜简单算美国项目；**Picovoice 是加拿大公司**，不是美国；**canary-qwen-2.5b 的 LLM 侧是 Qwen3-1.7B（中国阿里）**，把它算作纯美国模型会失准；Kyutai/Moshi 是法国。

---

## 九、给对比表的两条实操建议

1. **端侧可行性分档**（按量化后磁盘 + whisper.cpp 实测）：
   - **MCU 级（<1 MB～3.6 MB）**：Moonshine Micro（RP2350，470 KB RAM）
   - **手机/树莓派级（<200 MB）**：Whisper tiny/base/small q5（32/60/190 MB）、Moonshine v1 Tiny/Base（27M/61.5M）、Moonshine v2 Tiny/Small（33.6M/123M）、Picovoice Leopard/Cheetah（<20 MB）
   - **PC/Mac/车机级（0.5–1.1 GB）**：Whisper medium q5_0（539 MB）、**large-v3-turbo q5_0（574 MB，甜点）**、large-v3 q5_0（1.08 GB）、Moonshine v2 Medium（245M）
   - **需 GPU / 边缘服务器**：Parakeet-0.6B（≥2 GB RAM）、Canary-1B-v2（≥6 GB RAM）、Canary-Qwen-2.5B、distil-large-v3/v3.5（756M，无官方量化）

2. **精度-延迟 Pareto（同一 Open ASR Leaderboard 口径，Mean WER）**：canary-qwen-2.5b **5.63** < parakeet-v2 **6.05** < parakeet-v3 **6.32** < Moonshine v2 Medium **6.65** < canary-1b-v2 **7.15** < distil-large-v3.5 **7.21** < whisper-large-v3 **7.44** < distil-large-v3 **7.52** < whisper-large-v3-turbo **7.83** < Moonshine v2 Small **7.84** < distil-small.en **8.57** < Moonshine v1 Base **9.99** < Moonshine v2 Tiny **12.01**。
   **注意：这一列的 WER 可以横向比（同数据集同 normalizer），但同一张表里的 RTFx 绝对不能当端侧性能用——全部来自 1×H200。**

---

## 十、本次调研的已知缺口（未一手验证，勿臆测填充）

- Whisper **large-v2 的确切发布日**：GitHub releases 第 1、2 页均未出现 v2022* tag（第 1 页最早到 v20230307，第 2 页到 v20230117），2022-12 这个日期本次**未取得一手证据**。
- **Picovoice 总部城市/国别**：官网 company / careers / privacy 页均未给出，仅有 "© 2019-2026 Picovoice Inc."；Hackster 报道页返回 403。
- **Picovoice benchmark 的具体 WER 与 Core-Hour 数值**：官方页面以图表呈现，纯文本抓取取不到数字（数据在 github.com/Picovoice/speech-to-text-benchmark，Apache 2.0）。
- **Cartesia Sonic / Ink 的全部技术规格**：参数量、体积、延迟毫秒数、支持芯片、发布日期 —— 官网一律未披露。
- **NVIDIA 各模型卡上 RTFx 对应的具体 GPU**：模型卡本身没写（已用 Open ASR Leaderboard 仓库的 H200 说明作为间接一手证据补上）。
- **Parakeet v3 的 q8_0 GGUF 文件体积、NeMo-Speech.cpp 的端侧实测性能**：未披露。
- **Moonshine v2 各模型的量化文件 MB 体积**：文档只给相对量（per-channel scale 使下载体积 +0.5%），绝对值未披露。
- **OpenAI 官方博客（openai.com）**：403 无法抓取，"gpt-4o-transcribe 为 API-only、无新开放权重语音模型" 的结论基于 openai/whisper releases（一手）+ 多个 2026 年二手源交叉验证。